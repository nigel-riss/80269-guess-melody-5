const AuthorizationStatus = {
  AUTH: `AUTH`,
  NO_AUTH: `NO_AUTH`,
};

const GameType = {
  ARTIST: `artist`,
  GENRE: `genre`,
};

const MAX_MISTAKES_COUNT = 30;


export {
  AuthorizationStatus,
  GameType,
  MAX_MISTAKES_COUNT,
};
